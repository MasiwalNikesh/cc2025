# NPM Configuration for CORCON 2025

# Use legacy peer dependency resolution
# This resolves React 19 compatibility with older packages (like resend)
legacy-peer-deps=true

# Automatically save exact versions
save-exact=false

# Engine strict mode (optional)
engine-strict=false
